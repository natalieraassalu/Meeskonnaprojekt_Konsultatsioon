@page "/"
@* Stay on the server, don't move to the browser *@
@rendermode InteractiveServer
@using Microsoft.EntityFrameworkCore
@using Abc.Data.Consultation
@inject IDbContextFactory<ConsApp.Data.ApplicationDbContext> DbFactory

<PageTitle>Courses</PageTitle>

<h1>Available Courses</h1>
<p>Click a course to see available consultation slots.</p>
<hr />

@if (courses.Count == 0)
{
    <!-- Show this if no courses exist yet -->
    <p>No courses available yet.</p>
}
else
{
    <div class="row">
        @foreach (var course in courses)
        {
            <!-- Each course is shown as a card -->
            <div class="col-md-4 mb-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">@course.Name</h5>
                        <p class="card-text">@course.Details</p>
                        <!-- Clicking this button takes student to the course detail page -->
                        <a href="/courses/@course.Id" class="btn btn-primary">View Slots</a>
                    </div>
                </div>
            </div>
        }
    </div>
}

@code {
    // List of all courses loaded from database
    private List<Course> courses = new();

    // Runs when page loads - gets all courses
    protected override async Task OnInitializedAsync()
    {
        using var context = DbFactory.CreateDbContext();
        courses = await context.Course.ToListAsync();
    }
}