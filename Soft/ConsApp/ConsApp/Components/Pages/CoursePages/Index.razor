@page "/courses"
@using Microsoft.EntityFrameworkCore
@using Microsoft.AspNetCore.Components.QuickGrid
@using Abc.Data.Consultation
@using ConsApp.Data
@implements IAsyncDisposable
@inject IDbContextFactory<ConsApp.Data.ApplicationDbContext> DbFactory

<PageTitle>Index</PageTitle>

<h1>Index</h1>

<p>
    <a href="courses/create">Create New</a>
</p>

<QuickGrid Class="table" Items="context.Course">
    <PropertyColumn Property="course => course.Name" />
    <PropertyColumn Property="course => course.Code" />
    <PropertyColumn Property="course => course.Details" />
    <PropertyColumn Property="course => course.ValidFrom" />
    <PropertyColumn Property="course => course.ValidTo" />

    <TemplateColumn Context="course">
        <a href="@($"courses/edit?id={course.Id}")">Edit</a> |
        <a href="@($"courses/details?id={course.Id}")">Details</a> |
        <a href="@($"courses/delete?id={course.Id}")">Delete</a>
    </TemplateColumn>
</QuickGrid>

@code {
    private ApplicationDbContext context = default!;

    protected override void OnInitialized()
    {
        context = DbFactory.CreateDbContext();
    }

    public async ValueTask DisposeAsync() => await context.DisposeAsync();
}
